@page
@model DTO.U20180744.Web.Pages.NotasPage.NotasModel
@{
}

<h1>Listado de Notas</h1>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Id</th>
            <th>Materia</th>
            <th>Creditos</th>
            <th>Opciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Notas)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.IdAlumno</td>
                <td>@item.IdMateria</td>
                <td>@item.Nota</td>
                <td>
                    <a class="btn btn-sm btn-info" asp-area="" asp-page="/NotasPage/Editar" asp-route-id="@item.Id">Editar</a>
                    <a class="btn btn-sm btn-info" asp-area="" asp-page="/NotasPage/Eliminar" asp-route-id="@item.Id">Eliminar</a>
                </td>

            </tr>
        }
    </tbody>

</table>